package Chapter2;

public class task5 {

	public static void main(String[] args) {

		// 5.次の式のうち、コンパイルエラーになるものを選べ。(2つ選択)
		int $a = 100;
		int b_ = 200;
		int _0 = 300;
		// int ${d} = 400; ×
		// int g.a = 500;   ×

		/* 解説
		 * 変数やメソッド、クラスなどの名前のことを「識別子」と呼ぶ。基本自由に決められるが次の規則が存在する
		 * ・予約語を識別子として使うことは出来ない。
		 * ・使える記号は、アンダースコア「_」と通貨記号のみ
		 * ・数字から始めてはいけない（2文字目移行であれば使用可）
		 *
		 *
		 * ※予約語一覧
		 * abstract, assets, boolean, break, byte,
		 * case, catch, char, class, const, continue,
		 * default, do, double, else, enum, extends,
		 * final, finally, float, for, goto, if, emplements,
		 * import, instanceof, int, interface, long, native,
		 * new, package, private, protected, public, return,
		 * short, static, strictfp, super, switch, synchronized,
		 * this, throw, throws, transient, try, void, volatile, while, _(アンダースコア)
		 */
	}
}
